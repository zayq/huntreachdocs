---
title: 'Manual Connection'
description: 'Learn how to manually connect an email inbox to HuntReach via SMTP/IMAP for any provider.'
icon: 'gear'
---

---

## 🔗 Step 1: Access the email connection page

1. Go to the HuntReach email connection page:  
   [Email Connection](https://app.huntreach.com/dashboard/settings/emails)
2. Click on **"Connect a new email inbox"**, then select **"Custom Email"** for manual setup.

> ℹ️ **Tip**: Make sure your email provider allows SMTP/IMAP connections and that these options are enabled.

---

## 📝 Step 2: Fill out the form on HuntReach

Fill in the following fields:

| Field              | Description                                                                   |
| ------------------ | ----------------------------------------------------------------------------- |
| **Email**          | The email address you want to connect (example: `thomas@huntreach.net`).      |
| **Display Name**   | The display name shown in your emails (example: `Thomas Pecquet`).            |
| **Password**       | The account password or an app-specific password, if required.                |
| **IMAP Host**      | Your provider’s IMAP server (example: `imap.gmail.com`).                      |
| **IMAP Port**      | IMAP port (example: `993`).                                                   |
| **SMTP Host**      | Your provider’s SMTP server (example: `smtp.gmail.com`).                      |
| **SMTP Port**      | SMTP port (example: `465`).                                                   |
| **Warmup Enabled** | (optional) Indicate if you want warmup enabled for this address (TRUE/FALSE). |
| **Signature**      | (optional) Add a custom HTML signature or leave blank.                        |

Verify all information before proceeding.

> ⚠️ **Important**: If you're using two-factor authentication, you must generate an app-specific password for your account.

---

## ✅ Step 4: Connect the email address

1. Click the **"Connect Email"** button.
2. If everything is set up correctly, your email address will be successfully connected.

> 🎉 **Congratulations!** Your email inbox is now connected and ready to use with HuntReach.

---

## 🔍 Step 5: Troubleshooting common issues

If you run into issues while connecting:

| Issue                  | Solution                                                             |
| ---------------------- | -------------------------------------------------------------------- |
| **Connection error**   | Check your password and the IMAP/SMTP information provided.          |
| **SMTP/IMAP disabled** | Make sure these options are enabled in your email provider settings. |

> ❌ **Need help?** If the issue persists, contact HuntReach support for personalized assistance.
