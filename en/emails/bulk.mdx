---
title: 'Bulk Connection'
description: 'Add multiple sender email addresses to HuntReach using a CSV file.'
icon: 'upload'
---

---

## 📥 Step 1: Access the bulk import page

1. Go to the dedicated page:  
   👉 [HuntReach Bulk Import](https://app.huntreach.com/dashboard/email-accounts/bulk)
2. Download the CSV template here:  
   👉 [Google Sheets Template](https://docs.google.com/spreadsheets/d/1OoFFyXtlLmvQxu34yXs8aMvLaaFIS6ZRSJ4CLhLmL4w/edit?gid=784218695)

> ℹ️ **Tip**: Make a copy (`File → Make a copy`) to edit the file in your own Drive.

---

## 📝 Step 2: Fill out the file

Fill in the columns as follows:

| Column           | Description                                         |
| ---------------- | --------------------------------------------------- |
| **Email**        | Address to connect (e.g., `thomas@huntreach.net`)   |
| **Display Name** | Sender name shown (e.g., `Thomas from HuntReach`)   |
| **Password**     | Account password or application-specific password   |
| **IMAP Host**    | IMAP server (e.g., `imap.gmail.com`)                |
| **IMAP Port**    | Secure IMAP port (e.g., `993`)                      |
| **SMTP Host**    | SMTP server (e.g., `smtp.gmail.com`)                |
| **SMTP Port**    | Secure SMTP port (e.g., `465`)                      |
| **Warmup**       | TRUE / FALSE depending on whether warmup is enabled |
| **Signature**    | (Optional) Custom HTML signature or leave blank     |

---

## 📤 Step 3: Import the file

1. From Google Sheets:  
   `File → Download → Comma-separated values (.csv)`
2. On HuntReach:  
   Go back to the import page → **Import a CSV file** → select your file.

> ⚠️ **Important**: The CSV file must contain only one sheet.

---

## ✅ Step 4: Final check

- Verify the status of each email in HuntReach.
- If any fail, fix the data in the CSV and re-import.

---
